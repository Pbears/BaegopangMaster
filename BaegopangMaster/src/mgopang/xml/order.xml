<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mgopang.xml.order"> 
	<select id="getTotalRow" parameterType="java.util.HashMap" resultType="java.lang.Integer">
	
	SELECT COUNT(*) FROM FOODORDER
	WHERE STORENAME=#{storename}	
	
	</select>		
	
	<select id="selectOrder" resultType="order" parameterType="java.util.HashMap">
	
  		SELECT ORDERNUMBER, MEMBERID, MEMBERNAME, MEMBERTEL, MEMBERADDRESS, PRICE, NVL(ORDERINFO,'없음') ORDERINFO, 
  		MENUNAME, AMOUNT, STORENAME, STOREADDRESS, STORETEL, TO_DATE(ORDERTIME,'YYYYMMDD HH24:MI:SS')ORDERTIME, STATE
		FROM FOODORDER		
		WHERE STORENAME=#{storename}
		
  	</select>
	
     
     
</mapper>